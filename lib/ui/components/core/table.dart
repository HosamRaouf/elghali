import 'package:flutter/material.dart';

class Bonntable extends StatelessWidget {
  const Bonntable({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
