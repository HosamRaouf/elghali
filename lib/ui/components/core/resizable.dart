import 'package:flutter/material.dart';

class Bonnresizable extends StatelessWidget {
  const Bonnresizable({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
