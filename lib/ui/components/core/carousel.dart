import 'package:flutter/material.dart';

class Bonncarousel extends StatelessWidget {
  const Bonncarousel({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
