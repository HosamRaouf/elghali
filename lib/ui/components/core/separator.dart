import 'package:flutter/material.dart';

class Bonnseparator extends StatelessWidget {
  const Bonnseparator({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
