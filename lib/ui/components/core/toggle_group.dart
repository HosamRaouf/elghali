import 'package:flutter/material.dart';

class Bonntogglegroup extends StatelessWidget {
  const Bonntogglegroup({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
