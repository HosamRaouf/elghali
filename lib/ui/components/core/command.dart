import 'package:flutter/material.dart';

class Bonncommand extends StatelessWidget {
  const Bonncommand({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
