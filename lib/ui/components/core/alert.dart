import 'package:flutter/material.dart';

class Bonnalert extends StatelessWidget {
  const Bonnalert({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
