import 'package:flutter/material.dart';

class Bonncollapsible extends StatelessWidget {
  const Bonncollapsible({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
