import 'package:flutter/material.dart';

class Bonnform extends StatelessWidget {
  const Bonnform({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
