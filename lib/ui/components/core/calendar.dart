import 'package:flutter/material.dart';

class Bonncalendar extends StatelessWidget {
  const Bonncalendar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
