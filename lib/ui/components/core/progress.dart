import 'package:flutter/material.dart';

class Bonnprogress extends StatelessWidget {
  const Bonnprogress({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
