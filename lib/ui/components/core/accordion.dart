import 'package:flutter/material.dart';

class Bonnaccordion extends StatelessWidget {
  const Bonnaccordion({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
