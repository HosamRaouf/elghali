import 'package:flutter/material.dart';

class Bonndrawer extends StatelessWidget {
  const Bonndrawer({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
