import 'package:flutter/material.dart';

class Bonnskeleton extends StatelessWidget {
  const Bonnskeleton({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
