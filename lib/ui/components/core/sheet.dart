import 'package:flutter/material.dart';

class Bonnsheet extends StatelessWidget {
  const Bonnsheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
