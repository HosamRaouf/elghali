import 'package:flutter/material.dart';

class Bonnsonner extends StatelessWidget {
  const Bonnsonner({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
