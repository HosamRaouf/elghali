import 'package:flutter/material.dart';

class Bonnchart extends StatelessWidget {
  const Bonnchart({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
