import 'package:flutter/material.dart';

class Bonntoggle extends StatelessWidget {
  const Bonntoggle({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
