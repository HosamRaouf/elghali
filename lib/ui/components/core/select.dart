import 'package:flutter/material.dart';

class Bonnselect extends StatelessWidget {
  const Bonnselect({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
