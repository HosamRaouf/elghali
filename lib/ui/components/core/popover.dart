import 'package:flutter/material.dart';

class Bonnpopover extends StatelessWidget {
  const Bonnpopover({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
