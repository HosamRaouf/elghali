import 'package:flutter/material.dart';

class Bonntextarea extends StatelessWidget {
  const Bonntextarea({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
