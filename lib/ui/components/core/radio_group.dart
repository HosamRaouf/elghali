import 'package:flutter/material.dart';

class Bonnradiogroup extends StatelessWidget {
  const Bonnradiogroup({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
