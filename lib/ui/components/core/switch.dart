import 'package:flutter/material.dart';

class Bonnswitch extends StatelessWidget {
  const Bonnswitch({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
