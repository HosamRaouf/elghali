import 'package:flutter/material.dart';

class Bonndialog extends StatelessWidget {
  const Bonndialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
